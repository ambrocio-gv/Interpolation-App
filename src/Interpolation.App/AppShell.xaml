<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="Interpolation.App.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:Interpolation.App"
    Title="Interpolation.App">

    <Shell.TitleView>
        <Grid ColumnDefinitions="*" ColumnSpacing="10" Padding="8,0" BackgroundColor="#F5F5F5">
            <VerticalStackLayout Grid.Column="0" VerticalOptions="Center" Spacing="0">
                <Label Text="Interpolation Application"
                       FontSize="16"
                       FontAttributes="Bold"
                       TextColor="#333333"/>
                <Label Text="Engineering Tools"
                       FontSize="11"
                       TextColor="#666666"/>
            </VerticalStackLayout>
        </Grid>
    </Shell.TitleView>

    <ShellContent
        Title="Interpolation"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />

</Shell>
